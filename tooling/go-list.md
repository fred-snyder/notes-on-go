# Go list

```bash
# list the packages(/dependencies?)
go list

# list the current module and all its dependencies
go list -m all

# list available versions of a module
go list -m -versions rsc.io/sampler
```
